# Third-party runtime deps for the Mesh Host container. The Benzene layers themselves
# (benzene-results / -core / -http / -mesh) are installed from source in the Dockerfile pre-publish;
# once they are on PyPI, add them here (e.g. `benzene-mesh`, `benzene-http`) and drop the source COPY.
uvicorn>=0.30
